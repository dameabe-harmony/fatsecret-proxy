{
  "name": "fatsecret-proxy",
  "version": "1.0.0",
  "description": "Serverless proxy for FatSecret API to avoid CORS issues",
  "main": "api/barcode.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "oauth-1.0a": "^2.2.6",
    "crypto-js": "^4.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
